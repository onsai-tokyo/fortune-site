# 相性鑑定 v2.4 実装

採用版：editorial-v2.4-claude-structural。提供ZIPの engine/compose.py と3データを正本としてTypeScriptへ移植。bazi.jsonのみgzipで可逆圧縮し、展開後の原文バイト一致を確認。各プロセスで一度読み込み、順序付き日柱ペアと宿曜IDを索引化する。

## 接続

POST /partners/:id/compatibility の既存認証・予約・課金・原子的保存は維持。本人の保存済み計算値と相手の既存暦計算から日柱・宿を取得し、compacted context → relationForMansions → buildCompatibilityV24 → 7カード → 既存の二人の時期・命式 → readingSnapshot → complete_compatibility_operation へ接続。

新規相性は関係タイプにかかわらず指定順の7項目。AI再生成・要約・文字数切り詰めなし。title/headline/bodyの句読点と3段落を保持。追跡IDはmetadataRefsに保存し画面本文には出さない。人物交換は両日柱と両宿から再計算する。

## 版管理・既存保存

generatorVersionとruntime contextに版＋データ指紋を保存。索引／キャッシュ用識別子も版・順序付き入力・関係IDを含む。旧AI本文キャッシュは新規生成で使わない。完了済み操作の再送と既存鑑定の読み直しは保存済み本文をそのまま返し、無断更新・再課金しない。既存iOSの同一人物の履歴再利用も維持するため、保存済み鑑定を開くだけではv2.4にならない。

## 検証

- Python提供検証：期待270件・橋渡し23件・人物交換例、すべて一致。
- 移植先：期待出力全文一致、97,200パターンの7項目・3段落・字数範囲、729宿ペアの向き、不正入力拒否を確認。
- Python全件形式検査：680,400項目、519〜875文字、エラー0。新たな全件意味審査ではない。
- 本番ベースのビルド・24件の相性／保存経路／年間回帰テスト成功。
- iOSデコードテスト成功。シミュレーターで7カード順序、日本語見出しと本文、人物交換後の本文変化を確認。DEBUG仮入力プレビューは認証・購入・保存と独立し、製品の入力には使用しない。

性格・年運・既存相性の時期判定は変更しない。本番の認証付きユーザー鑑定生成はテストで消費していない。
